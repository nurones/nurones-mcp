# Releases
- Tag vMAJOR.MINOR.PATCH → GitHub Actions builds artifacts (see .github/workflows/release.yml).
- v0.5.4: Unified single port 50550; Admin UI served from MCP; Docker + Compose added; IDE plugins separated to plugins/; Extensions tab groups tools by manifest entry.
